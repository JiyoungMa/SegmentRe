spring:
  datasource:
    url: jdbc:mysql://localhost:3306/segmentTest
    username: devcourseTester
    password: tester!

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
        default_batch_fetch_size : 100